# Ebook Neo Sync View Visual Evidence

- Changed files: `src/App.tsx`, `src/App.css`, `src/App.test.tsx`, `src/manifest.ts`, `src/manifest.test.ts`, `src-tauri/src/manifest.rs`, `src-tauri/src/lib.rs`, `README.md`.
- Route or URL: `http://127.0.0.1:1422/` with a Tauri invoke/event mock.
- Viewport: `1180x760`.
- Artifact filename: `ebook-neo-sync-view.png`.
- Observed result: the `同步` tab renders with scan/start controls, shows valid/missing/outdated/extra counts after scanning, lists pending sync files and extra local files, and keeps the download side panel visible without obvious overlap. Browser runtime/log errors and warnings: 0.
